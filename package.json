{
  "name": "electron-auto-update-example",
  "version": "1.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/merces-app/test.git"
  },
  "author": "TA",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/merces-app/test/issues"
  },
  "homepage": "https://github.com/merces-app/test#readme",
  "devDependencies": {
    "electron": "^11.2.0",
    "electron-builder": "^22.9.1"
  },
  "dependencies": {
    "electron-updater": "^4.3.5"
  }
}
